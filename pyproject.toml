[project]
name = "mobility-rfp-agent"
version = "0.1.0"
description = "Draft Proposal Agent for mobility fleet RFPs"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.111.0",
    "uvicorn[standard]==0.30.0",
    "gunicorn==22.0.0",
    "pydantic==2.7.4",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
